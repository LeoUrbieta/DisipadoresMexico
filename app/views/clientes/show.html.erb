<h1>Compras del Cliente: <%= @cliente.nombre %>, ID_Cliente: <%= @cliente.id%></h1>


<br>
<br>

<table class="table table-striped table-hover table-condensed table-bordered">
  <tr>
    <th>Id Venta</th>
    <th>Fecha</th>
    <th>Total Pagado</th>
  </tr>
  
  <% @cliente.ventas.each do |venta| %>
    <tr>
      <td><%= venta.id%></td>
      <td><%= venta.fecha %></td>
      <td><%= venta.total_pagado_por_cliente %></td>
      <td><%= link_to 'Ver esta venta', edit_venta_path(venta), class: 'btn btn-info' %></td>
    </tr>
  <% end %>
  
</table>

<p><%= link_to 'Ir a Lista de Clientes', clientes_path, class: 'btn btn-info' %></p>
<p><%= link_to 'Ir a Todas las Ventas', ventas_path, class: 'btn btn-warning' %></p>
<p><%= link_to "Regresar a Página Anterior", url_for(:back), class:'btn btn-success' %></p>